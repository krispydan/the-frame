export const dynamic = "force-dynamic";

/**
 * Designer-queue view — Phase 4 build. Phase 1 stub so the link
 * from /marketing/email doesn't 404.
 */
export default function DesignerQueuePage() {
  return (
    <div className="space-y-2">
      <h1 className="text-3xl font-bold tracking-tight">Designer Queue</h1>
      <p className="text-muted-foreground">
        Image briefs + upload dropzones — Phase 4 build.
      </p>
    </div>
  );
}
