export const dynamic = "force-dynamic";

/**
 * Month calendar view — Phase 5 build. Phase 1 stub so the link
 * from /marketing/email doesn't 404.
 */
export default function EmailCalendarPage() {
  return (
    <div className="space-y-2">
      <h1 className="text-3xl font-bold tracking-tight">Email Calendar</h1>
      <p className="text-muted-foreground">
        Month-grid view coming in Phase 5. Use the dashboard for now.
      </p>
    </div>
  );
}
